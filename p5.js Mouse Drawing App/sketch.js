function setup()
{
	createCanvas(800, 600);
	background(255);
}



function draw()
{
    fill(0);             // لون أسود
    noStroke();          // بدون حدود
    ellipse(mouseX, mouseY, 10, 10);
   
}
